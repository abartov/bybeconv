# Common environment variables for test environment.
# Those settings assumes that we host all used services using docker-compose.yml
# if you need to override some vars with values specific to your machine, create an `.env.test.local` file and
# set values there.

DATABASE_URL=mysql2://root:root@127.0.0.1:13306/bybe_test