%h1 Listing lex_entries

%table
  %thead
    %tr
      %th Title
      %th Status
      %th

  %tbody
    - @lex_entries.each do |lex_entry|
      %tr
        %td= lex_entry.title
        %td= lex_entry.status
        %td= link_to 'Show', lexicon_entry_path(lex_entry)

%br
