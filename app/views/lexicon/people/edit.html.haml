%h1 Editing lex_person

= render partial: 'lexicon/entries/tabs'

.tab-content
  .tab-pane.fade.show.active#properties
    = render 'form'
  = render partial: 'lexicon/entries/attachments', locals: { entry: @lex_person.entry }
  = render partial: 'lexicon/entries/citations', locals: { entry: @lex_person.entry }
