<% if @publication.status == 'uploaded' %>
name = "pub<%= @publication.id %>";
if($('#'+name).length > 0) {
  $('#'+name).remove();
} else if($('.'+name).length > 0) {
  $('.'+name).remove();
}
<% end %>