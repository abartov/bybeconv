$('#collection_authorities').append('<%= j render(partial: 'ingestible_collection_authorities/authority', locals: { authority: @authorities.last, editable: true }) %>');
$('#add_collection_authority').val('');
$('#collection_authority_id').val('');
$('#add_collection_authority_name').val('');
$('#new_collection_person').val('');
// If mirroring occurred, also update default authorities display
<% if @ingestible.default_authorities == @ingestible.collection_authorities %>
$('#authorities').html('<%= j render(partial: 'ingestible_authorities/authority', collection: @authorities, locals: { editable: true, toc: false }) %>');
<% end %>
